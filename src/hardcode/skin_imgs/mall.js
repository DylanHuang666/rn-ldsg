'use strict'

import Config from "../../configs/Config";


export const top_bg = () => ({ uri: Config.getRNImageUrl('mall/top_bg.png', 0) });

export const ic_triangle = () => ({ uri: Config.getRNImageUrl('mall/ic_triangle.png', 0) });
